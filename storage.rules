rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /shoe-images/{allPaths=**} {
      allow read, write: if request.auth != null
        && (request.resource == null
          || (request.resource.size < 5 * 1024 * 1024
            && request.resource.contentType.matches('image/.*')));
    }
  }
}
